# 7iter mockup site

Open `docs/index.html` in a browser or publish the repository with GitHub Pages using `/docs` as the source.

This site contains direct review mockups for item1~4 and links to item2 SG90 clicker CAD files.
